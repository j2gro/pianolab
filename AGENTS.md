# Agent notes

## Tracking

All work items go on the **Kanban Markdown** board (`.devtool/features/`). Create or reuse a card **before** implementing. Follow `.cursor/rules/kanban-markdown.mdc` and `.cursor/skills/kanban-markdown/SKILL.md`.
